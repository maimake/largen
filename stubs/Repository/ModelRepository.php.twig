<?php

namespace <%=namespace%>;

use Maimake\Largen\Support\Repositories\ModelRepository;
use <%= model_class %>;

class <%= classname %> extends ModelRepository
{
    protected $modelClass = <%= model_name %>::class;

    public function create($data)
    {
        return \DB::transaction(function () use ($data) {
            $model = $this->modelClass::create($data);
            // do something
            return $model;
        });
    }

}
