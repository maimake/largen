<?php

namespace <%=namespace%>;

use <%= client_classname %>;

class <%=classname%>
{
    protected $client;

    public function __construct(<%= client_name %> $client)
    {
        $this->client = $client;
    }
}
