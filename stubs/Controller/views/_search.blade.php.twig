<?php

/**
 * @var <%= model_classname %> $<%= model_singular %>
 */

?>

<div class="<%= model_plural %>-search">

    {{Form::model($<%= model_singular %>, ['route' => ['<%= model_plural %>.index'], 'method' => 'GET'])}}

    <% if has_user %>
    <div class="form-group">
        {{Form::label('username', 'User')}}
        <select class="form-control username-select" id="username" name="searchModel[username]" style="width: 100%">
            <option></option>
            @isset($searchModel->username)
                <option selected>{{$searchModel->username}}</option>
            @endisset
        </select>
    </div>
    <% endif %>

    <% for column in columns %>
    <div class="form-group">
        {{Form::label('<%= column %>', humanize_case('<%= column %>'))}}
        {{Form::text('<%= column %>', null, ['class' => 'form-control', 'name' => 'searchModel[<%= column %>]'])}}
    </div>
    <% endfor %>

    <div class="form-group">
        {{Form::submit('Search', ['class' => 'btn btn-primary'])}}
        {{Form::reset('Reset', ['class' => 'btn btn-default'])}}
    </div>

    {{Form::close()}}
</div>
