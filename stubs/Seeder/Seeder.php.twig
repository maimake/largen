<?php

<% if namespace is not empty %>
namespace <%=namespace%>;
<% endif %>

use Illuminate\Database\Seeder;
use <%= model_class %>;

class <%=classname%> extends Seeder
{
    /**
     * Run the database seeds.
     *
     * @return void
     */
    public function run()
    {
        DB::transaction(function () {
            factory(<%= model_name %>::class, 50)->create();

//            <%= model_name %>::firstOrCreate(
//                [
//                    'name' => 'xxx',
//                ],
//                [
//                    'name' => 'xxx',
//                ]);
        });
    }
}
